<div class="featured-projects-container">
  {% for project in site.data.featured_projects %}
    <a class="featured-project-card" href="{{ project.url }}" target="_blank" rel="noopener noreferrer">
      <span class="featured-project-title">{{ project.title }}</span>
    </a>
  {% endfor %}
</div>
